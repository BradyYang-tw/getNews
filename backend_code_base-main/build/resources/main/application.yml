spring:
  datasource:
    url: jdbc:postgresql://192.168.68.103:5432/getNews_db
    username: brady
    password: brady
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080