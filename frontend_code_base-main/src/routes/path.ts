export const APP_PREFIX = '/rd-lab'

export const PATH: Record<string, string> = {
  LOGIN: `${APP_PREFIX}/login`,
  REDIRECT_PAGE: `${APP_PREFIX}/redirect`,

  PROJECT_MANAGEMENT: `${APP_PREFIX}/project-management`,
}
